dataset:
  name: metaworld
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 1
  keys_to_load:
    - pixels
    - depth
    - tactile
    - proprio
    - action
  keys_to_cache:
    - action
    - proprio
